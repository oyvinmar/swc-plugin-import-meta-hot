{
  "name": "swc-plugin-import-meta-hot",
  "version": "0.1.3",
  "description": "@swc plugin for handling the transformation of import.meta.hot and import.meta.env",
  "author": "Øyvind Marthinsen",
  "homepage": "https://github.com/oyvinmar/swc-plugin-import-meta-hot#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/oyvinmar/swc-plugin-import-meta-hot.git"
  },
  "license": "Apache-2.0",
  "keywords": [
    "swc-plugin",
    "import.meta"
  ],
  "main": "swc_plugin_import_meta_hot.wasm",
  "scripts": {
    "build": "cargo build-wasip1",
    "build:release": "pnpm build --release",
    "build:test": "cargo test --no-run",
    "preinstall": "npx only-allow pnpm",
    "prepublishOnly": "cargo build-wasip1 --release",
    "prepack": "pnpm build:release && cp target/wasm32-wasip1/release/swc_plugin_import_meta_hot.wasm .",
    "release": "release-it",
    "test": "pnpm test:setup && pnpm test:unit && pnpm test:integration",
    "test:setup": "pnpm build:test && pnpm build && cp target/wasm32-wasip1/debug/swc_plugin_import_meta_env.wasm .",
    "test:unit": "cargo test --color always",
    "test:integration": "pnpm exec jest --config ./tests/integration/jest.config.cjs --no-cache"
  },
  "files": [
    "swc_plugin_import_meta_hot.wasm"
  ],
  "preferUnplugged": true,
  "devDependencies": {
    "@swc/core": "^1.15.1",
    "@swc/jest": "^0.2.39",
    "@types/jest": "^30.0.0",
    "auto-changelog": "^2.5.0",
    "jest": "^30.2.0",
    "release-it": "^19.0.6"
  },
  "peerDependencies": {
    "@swc/core": "^1.15.1"
  }
}
